const assert = require('chai').assert;
const airCo2SensorMock = require('../server/airCo2SensorMock');

describe('airCo2SensorMock', function () {

  it('exists', function () {
    assert(airCo2SensorMock);
  })

});
